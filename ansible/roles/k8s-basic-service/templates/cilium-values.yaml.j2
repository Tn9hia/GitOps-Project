# cilium-values.yaml
ipam:
  mode: kubernetes

kubeProxyReplacement: true

k8sServiceHost: "{{ hostvars['haproxy']['ansible_host'] }}"
k8sServicePort: "6443"

bpf:
  masquerade: true

operator:
  replicas: 1

hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true
